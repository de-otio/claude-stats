# Dependabot version-update configuration.
#
# Security updates are governed by the repo-level "Dependabot security
# updates" setting and run independently of this file.
#
# Each Dependabot PR (security or version) is reviewed by the org-shared
# `dependabot-claude-review.yml` workflow (see
# .github/workflows/dependabot-handler.yml), which checks for supply-chain
# attack signals and enables GitHub auto-merge when the verdict is APPROVE.
# GitHub then merges the PR once required status checks on `master` pass.

version: 2
updates:
  # ── npm root workspace (covers every packages/* via npm workspaces) ────────
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 10
    labels:
      - dependencies
    commit-message:
      prefix: "deps"
      include: scope
    groups:
      # Group routine non-major bumps so we don't drown in PRs.
      # Major bumps stay individual — the review workflow short-circuits
      # majors to `needs-human-review` regardless of grouping.
      patch-and-minor:
        update-types:
          - minor
          - patch
      # Keep AWS / Anthropic / VS Code / type-defs / dev tooling in
      # tighter, separate groups so a bad bump in one area doesn't
      # block unrelated updates.
      aws:
        patterns:
          - "aws-*"
          - "@aws-*"
          - "@aws-amplify/*"
          - "@aws-sdk/*"
        update-types:
          - minor
          - patch
      anthropic:
        patterns:
          - "@anthropic-ai/*"
        update-types:
          - minor
          - patch

  # ── VS Code extension runtime deps ────────────────────────────────────────
  # extension/ is deliberately outside the npm workspaces (its deps ship inside
  # the VSIX and must not be hoisted into the repo root), so the "/" entry above
  # does not see it. Without this entry nothing watches @huggingface/transformers
  # or onnxruntime-node — the two packages that actually reach users' machines.
  #
  # Note: scripts/prepare-vsix.mjs installs these with --no-package-lock, so
  # Dependabot can only offer updates for the declared ranges; it cannot see the
  # transitive tree, and no security alert will ever fire for one. Adding a
  # lockfile here would close that gap and make the VSIX build reproducible.
  - package-ecosystem: npm
    directory: "/extension"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 5
    labels:
      - dependencies
    commit-message:
      prefix: "deps"
      include: scope

  # ── GitHub Actions ────────────────────────────────────────────────────────
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Berlin
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: "ci"
